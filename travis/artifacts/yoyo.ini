[DEFAULT]

sources = %(here)s/migrations

# Target Database
# Database needs to be created
database = mysql://root@localhost/manowar2?unix_socket=/var/run/mysqld/mysqld.sock

# Verbosity
verbosity = 3

# Batch Mode
batch_mode = on
